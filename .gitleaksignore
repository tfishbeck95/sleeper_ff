# Findings that have been checked and are not secrets.
#
# Each line is a gitleaks fingerprint: commit, file, rule, line. That shape is what makes an entry
# here safe — it allowlists one finding in one commit, so it can never mask a real secret added to
# the same file later.
#
# This file belongs on the default branch rather than only on the branch that introduced a finding.
# The scan runs with `fetch-depth: 0`, which fetches every ref, so gitleaks reads commits from every
# branch in the clone: a finding on one branch fails the secret scan on every pull request until the
# allowlist reaches main. Removing the commit would not help either — GitHub keeps a merged or closed
# pull request's `refs/pull/*` refs indefinitely, so a rewrite leaves the object reachable.
#
# Nothing is added here without being verified first. A finding that is a real secret is rotated and
# the history is rewritten; an entry below means somebody read it and established it was neither.

# A fixture in the log-redaction test on claude/api-security-hardening-ummix7 (#16): an invented
# string with the shape of a provider key, present so the test can prove the redactor removes one. It
# authenticates nothing and never did, so there is nothing to rotate. The literal was replaced with an
# inert placeholder in the following commit, but the scan reads history — correctly, since a secret is
# compromised from the commit that introduced it — so removing it from the tip does not clear it.
0e62ff88c1b2527790f11e242119d58d13119d30:apps/api/src/log.test.ts:generic-api-key:30

# A scrypt salt in the end-to-end stack's fixture account (`Add PostgreSQL storage and E2E coverage`).
# The line hashes the literal password on the line above it, so what was flagged is the salt — sixteen
# hex characters, chosen to be obviously inert, feeding a hash the test suite hands to a throwaway
# process it starts and then kills. A salt is not a secret in the first place: it is stored beside the
# hash by design, and this one guards a password that is printed in the same file. It authenticates
# nothing, here or anywhere.
fa4d5a9833796f62d3f3ac6c056dfde46a3a6bac:tests/support/stack.mjs:generic-api-key:15
