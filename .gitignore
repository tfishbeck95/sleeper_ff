node_modules/
dist/
# Every local environment file, at any depth: the root `.env` and `deploy/.env*` all carry credentials.
# The examples are the documentation and are tracked.
.env
.env.*
!.env.example
!.env.staging.example
*.log
data/*.json
!data/.gitkeep
# TypeScript's incremental build cache. A build artifact, and a stale one committed is how a build
# behaves differently in CI than it does in a checkout.
*.tsbuildinfo
