import { FormEvent, useState } from 'react';
import { post, setCsrfToken } from './dashboard/api';
export function Login({onLogin}:{onLogin:()=>void}) { const [login,setLogin]=useState('admin'),[password,setPassword]=useState(''),[error,setError]=useState(''); const submit=async(e:FormEvent)=>{e.preventDefault();setError('');try{const result=await post<{csrfToken:string}>('/auth/login',{login,password});setCsrfToken(result.csrfToken);onLogin();}catch(value){setError(value instanceof Error?value.message:'Login failed.');}}; return <main className="onboarding-shell"><div className="onboarding-main"><div className="intro"><p className="kicker">Private access</p><h1>Sign in to Huddle.</h1><p>Your Sleeper username is linked only after application authentication.</p></div><form className="connect-card panel" onSubmit={e=>void submit(e)}><label htmlFor="login">Login</label><input id="login" autoComplete="username" value={login} onChange={e=>setLogin(e.target.value)}/><label htmlFor="password">Password</label><input id="password" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)}/><button disabled={!login||!password}>Sign in</button>{error&&<p role="alert">{error}</p>}</form></div></main>; }
